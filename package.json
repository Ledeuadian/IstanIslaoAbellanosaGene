{
  "name": "family-tree-abellanosa",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive 3D Family Tree Visualization with Infinite Zoom and Branching",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "dev:web": "turbo dev --filter=web",
    "dev:api": "turbo dev --filter=api"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "vite-plugin-pwa": "^1.3.0"
  }
}
